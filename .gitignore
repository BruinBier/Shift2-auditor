# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env
# Ook reservekopieen: .env2 stond hier een maand buiten, met het
# databasewachtwoord erin. Een `git add -A` had dat gepubliceerd.
.env?
.env.backup*

# Tussenbestanden van het uitproberen van de Word-export.
debug-*.xml
debug-*.txt

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts


# visual snapshots from crawler tests
public/__visual-snapshots__/

# temporary test files
temp-*.txt
test-*.html
nul

# Werkmap voor alles wat tijdens een audit tijdelijk wordt weggeschreven:
# opgehaalde pagina's, screenshots, PDF-uitsneden, meetscripts en tussenresultaten.
/tmp/

# Claude Code local settings (per developer)
.claude/settings.json

# Werkkopieen van het project die een agentsessie met isolation: worktree aanmaakt.
# Git ziet daar een eigen .git in en registreert de map anders als gitlink (mode
# 160000), alsof het een submodule is -- terwijl er geen .gitmodules bestaat. Er
# stonden er twee in de index, en die meldden zich bij elke sessie als "modified"
# zodra een andere sessie daar verderwerkte. Dat vervuilt `git status`, en juist
# daarop leunt de controle na een Work-sessie waarin Codex meedeed.
.claude/worktrees/

# private files (blijven lokaal op schijf)
Instructie_Toegankelijke_Videos.docx
tmp_proj.json

# database backups (lokaal, niet in Git - kan groot zijn en bevat data)
/backups/



# Auditbewijs: de opgeslagen HTML en screenshots die capture-sample-evidence
# per steekproefitem wegschrijft. Gegenereerd bewijs dat bij elke audit opnieuw
# ontstaat, en het zijn hele webpagina's -- dat hoort niet in de geschiedenis.
public/uploads/

# Skills in het OpenAI-formaat. Dezelfde skills staan al in .claude/skills/;
# dit is een tweede kopie voor Codex, en die doet geen ontwikkelwerk meer.
# Zie docs/adr/0004-codex-mag-alleen-lezen.md.
.agents/
